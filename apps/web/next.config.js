/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The App Router is enabled by default in Next.js 14; no need for
  // experimental.appDir. Removing unrecognized options prevents build warnings.
};

module.exports = nextConfig;